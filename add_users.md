Me and another friend will use this application in parallel in the same dataset, so we need to add users to the application. Each user will have their own workspace and will be able to access the same dataset without interfering with each other's work.

My Idea is, before starting generate the .csv relation only with the name of the images in the dataset and the user that that image belongs to. Then, when the user start need to have a placeholder to select which person is working, and then the app will load only the images that belongs to that user.

