Do NOT TAKE TOKENS READING THE entire DATSET, JUST READ A FEW IMAGES TO UNDERSTAND THE STRUCTURE OF THE DATASET AND THEN ADD THE NEW FEATURE.   

Now I will audit a dataset that has original masks beside the masks generated by SAM3. Could you add a feature that verrify if the dataset folder has a /original_masks/ folder, and if it does, read the original masks and compare them with the SAM3 generated masks. Note that in this case, you should not show me the original bbox, only the masks is already sufficient.

Now, I will have 3 options to choose from:
1. Delete the image (output in the .csv = nothing)
2. Keep the original mask (output in the .csv = original_mask)
3. Keep the SAM3 generated mask (output in the .csv = SAM3_mask or SAM3_mask_cleaned). Note that i still want to have the option to clean the SAM3 generated mask

If the dataset folder does not have a /original_masks/ folder, then the process will be the same as before.

An example of dataset with /original_masks/ folder is the following:
dataset/
├── images/
│   ├── image1.jpg
├── original_masks/
│   ├── image1.jpg
|── sam_masks/
    ├── image1.jpg
You can still creating the folder for sam_masks_cleaned/

Please, make sure to follow the same process as before, so Read the code and undestand the structure of the code, and then add the new feature accordingly. An example of the output .csv file withou /original_masks/ folder is the following:
RDD022_Potholes_SAM3/audit_results_Caio.csv



